import { Meta, Story } from "@storybook/web-components";
import { html } from "lit-html";

export default {
  title: "AppMenu/AppMenuBody",
} as Meta;

export const Default: Story = () => {
  return html`<app-menu-body></app-menu-body>`;
};
